{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "go",
            "request": "launch",
            "program": "${workspaceFolder}/cmd/main.go",
            "env": {
                "APP_ENV": "development"
            }
        }
    ]
}